{
  "name": "geological-history-tool",
  "version": "1.0.0",
  "private": true,
  "description": "GeoGlobe: an interactive globe of continental drift from the Cambrian to today. The site itself has no build step and no runtime dependencies; jsdom is used only by the smoke test.",
  "license": "(MIT AND CC-BY-4.0)",
  "scripts": {
    "test": "node test/smoke.js",
    "links": "node test/links.js"
  },
  "devDependencies": {
    "jsdom": "^29.1.1"
  }
}
